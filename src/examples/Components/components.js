import Welcome from './welcome';

function Components() {
  return (
    <div>
      <h1> Components in React</h1>
      <Welcome />
      <p>This is a parent component rendering a child component.</p>
    </div>
  );
}

export default Components;
